package org.spartaglobal.models;

/**
 * @implNote This is the sorter interface used for all other sorting classes
 *           only contains one method which returns an int array
 */
public interface ISorter {
    int[] sortArray();
}
